mixin box-actions(link, list)
	div(
		class=`flex justify-center py-[10px]`
		box-actions = link
	)
		select(
			name=""
			class=`flex-1 outline-none pl-[10px] text-adminColorHeadline bg-transparent`
		)
			each it in list
				option(value=it.value class=`text-adminColorHeadline`) #{it.lable}
		button(
			type="button"
			class=`bg-adminColorTertiary text-adminColorHeadline text-[12px] rounded-[10px] px-[10px]`
		) Cập nhật